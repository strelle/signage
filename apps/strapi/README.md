# Strapi (signage)

Strapi v5 app built via multi-stage Dockerfile.

Build & publish: .github/workflows/build-publish.yml (GHCR)
Deploy: .github/workflows/deploy.yml (SSH → NAS → docker compose pull/up)
