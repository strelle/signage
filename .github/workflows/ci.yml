name: ci

on:
  pull_request:
  push:
    branches: [main]

jobs:
  docs-contract:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Validate project documentation contract
        run: .github/scripts/check-docs-contract.sh
